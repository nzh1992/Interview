一、哪些操作会导致python内存溢出，怎么处理？
1. 内存中加载的数据量过于庞大，如一次从数据库去除过多数据，比如数据查询未做分页处理。
2. 集合类中有对象的引用，使用完后未清空
3. 循环引用

二、Python的内存管理机制及调优手段？
1. 内存管理机制：引用计数、垃圾回收、内存池
    引用计数：引用计数是一中非常搞笑的内存管理手段，当一个python对象对引用时，其引用计数+1，当不再被某一个变量引用时，其引用计数-1
            当引用计数为0时，对象会在下一次GC时被回收。注意，弱引用不会增加引用计数。

